# 如何在不同页面中插入序言

## 简介

本文档介绍如何在不同页面中自由插入三个语言版本的序言文件。所有序言文件均采用 `::: tip` 样式，确保在不同页面中保持一致的视觉效果。

## 序言文件位置

我们已经创建了以下语言版本的序言文件：

- **英文版本**：`/en/prologue.md`
- **简体中文版本**：`/zh-CN/prologue.md`
- **繁体中文版本**：`/zh-TW/prologue.md`
- **默认版本**（简体中文）：`/prologue.md`

## 插入方法

### 方法一：使用 VitePress 的代码块包含指令

在 VitePress 中，你可以使用代码块的特殊语法来插入其他文件的内容。注意：这种方式会将内容作为代码块显示，不会渲染 `::: tip` 样式。

```markdown
<!-- 插入简体中文序言 -->
<<< @/prologue.md

<!-- 插入英文序言 -->
<<< @/en/prologue.md

<!-- 插入繁体中文序言 -->
<<< @/zh-TW/prologue.md
```

### 方法二：使用 Prologue 组件（推荐）

我们已经创建了一个 `Prologue` 组件，它可以正确渲染 `::: tip` 样式的序言内容：

#### 步骤1：在页面中使用组件

```markdown
# 项目白皮书

## 序言

<!-- 使用默认语言（简体中文）的序言 -->
<Prologue />

<!-- 或者指定语言 -->
<Prologue lang="en" />
<Prologue lang="zh-TW" />

## 核心价值观

...
```

## 示例：在不同语言页面中插入对应语言的序言

### 英文页面示例 (`/en/Whitepaper.md`)

```markdown
# Human Health Plan Foundation White Paper

## Prologue

<Prologue lang="en" />

## Core Values

...
```

### 简体中文页面示例 (`/zh-CN/Whitepaper.md`)

```markdown
# 人类健康计划基金会白皮书

## 序言

<Prologue />

## 核心价值观

...
```

### 繁体中文页面示例 (`/zh-TW/Whitepaper.md`)

```markdown
# 人類健康計劃基金會白皮書

## 序言

<Prologue lang="zh-TW" />

## 核心價值觀

...
```

## 自定义样式

所有序言文件均使用 `::: tip` 样式，如果你需要自定义样式，可以在 `.vitepress/theme/index.css` 文件中添加以下样式：

```css
/* 自定义序言提示块样式 */
.VPContent .tip {
  background-color: rgba(51, 153, 0, 0.1);
  border-left: 4px solid #339900;
  padding: 20px;
  margin: 20px 0;
  border-radius: 4px;
}

.VPContent .tip-title {
  color: #339900;
  font-weight: 600;
  margin-bottom: 12px;
}
```

## 最佳实践建议

1. **保持一致性**：
   - 在同一语言的页面中，始终使用对应语言版本的序言文件
   - 确保所有序言文件的内容结构一致，只是语言不同

2. **内容更新**：
   - 当需要更新序言内容时，只需修改对应的序言文件，所有引用该文件的页面都会自动更新
   - 建议定期检查序言内容，确保与项目最新状态保持一致

3. **多语言管理**：
   - 使用相同的文件命名和目录结构来组织不同语言版本的序言文件
   - 考虑使用自动化工具来同步不同语言版本之间的内容更新

4. **性能考虑**：
   - 序言文件不宜过长，避免影响页面加载速度
   - 对于大型文档，考虑使用分页或折叠的方式来展示序言内容

## 示例效果

### 简体中文序言效果

<Prologue />

### 英文序言效果

<Prologue lang="en" />

### 繁体中文序言效果

<Prologue lang="zh-TW" />

## 总结

通过创建单独的序言文件并使用我们提供的 `<Prologue />` 组件，你可以在不同页面中自由插入序言内容，同时保持内容的一致性和可维护性。

这种方法的优点包括：

1. **内容集中管理**：所有序言内容集中在单独的文件中，便于统一更新和维护
2. **多语言支持**：为不同语言版本的页面提供对应语言的序言内容
3. **样式一致性**：所有序言使用相同的 `::: tip` 样式，确保视觉效果一致
4. **灵活性**：可以根据需要在任何页面中插入任何语言版本的序言
5. **自动渲染**：`<Prologue />` 组件会自动渲染 `::: tip` 样式，确保视觉效果正确显示

希望本文档对你有所帮助！
