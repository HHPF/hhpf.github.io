# 递归式编程规则

## 核心原则

1. **明确终止条件**：每个递归函数必须有清晰的终止条件，避免无限递归
2. **单次调用规模递减**：每次递归调用必须使问题规模缩小
3. **保持状态简洁**：递归函数的参数应尽可能少，避免传递不必要的状态
4. **优先考虑尾递归**：尾递归优化可以避免栈溢出，提高性能
5. **适当使用记忆化**：对于重复计算的问题，使用记忆化缓存结果

## 代码示例

### 1. 基本递归结构

```javascript
// 阶乘计算示例
function factorial(n) {
  // 终止条件
  if (n === 0 || n === 1) {
    return 1;
  }
  // 递归调用，问题规模递减
  return n * factorial(n - 1);
}
```

### 2. 尾递归优化

```javascript
// 尾递归优化的阶乘计算
function factorialTail(n, accumulator = 1) {
  // 终止条件
  if (n === 0 || n === 1) {
    return accumulator;
  }
  // 尾递归调用，结果通过累加器传递
  return factorialTail(n - 1, n * accumulator);
}
```

### 3. 记忆化优化

```javascript
// 斐波那契数列计算，使用记忆化优化
function fibonacci(n, memo = {}) {
  // 终止条件
  if (n <= 1) {
    return n;
  }
  // 检查缓存，避免重复计算
  if (memo[n]) {
    return memo[n];
  }
  // 递归调用并缓存结果
  memo[n] = fibonacci(n - 1, memo) + fibonacci(n - 2, memo);
  return memo[n];
}
```

### 4. 二叉树遍历

```javascript
// 二叉树的先序遍历
function preorderTraversal(node, result = []) {
  // 终止条件：节点为空
  if (!node) {
    return result;
  }
  // 处理当前节点
  result.push(node.value);
  // 递归遍历左子树
  preorderTraversal(node.left, result);
  // 递归遍历右子树
  preorderTraversal(node.right, result);
  return result;
}
```

## 最佳实践

1. **避免深度过大**：对于可能产生深层递归的问题，考虑使用迭代或其他算法
2. **测试边界条件**：确保终止条件在所有情况下都能正确触发
3. **保持函数纯度**：递归函数应尽量保持纯函数特性，便于测试和调试
4. **清晰命名**：函数名和参数名应清晰表达递归的意图
5. **适当注释**：关键的递归逻辑应添加注释，说明递归的目的和终止条件

## 常见错误

1. **缺少终止条件**：导致无限递归和栈溢出
2. **问题规模未递减**：同样导致无限递归
3. **过度使用递归**：对于简单问题，迭代可能更高效
4. **忽略尾递归优化**：在处理大数据时导致性能问题
5. **内存泄漏**：未正确释放递归过程中创建的临时对象

## 适用场景

- 树形结构遍历
- 分治算法（如快速排序、归并排序）
- 回溯算法（如八皇后问题、迷宫求解）
- 动态规划问题（如斐波那契数列、背包问题）
- 函数式编程中的递归函数

递归是一种强大的编程技术，遵循上述规则可以编写高效、可靠的递归代码。在实际开发中，应根据问题特点选择合适的算法，递归并非总是最佳选择。